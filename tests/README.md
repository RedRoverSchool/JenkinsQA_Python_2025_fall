## Регламент написания автотестов

### 1. Группировка тестов

Чтобы избежать большого количества тестов в одном файле, все тесты разбиваются на логические подгруппы в соответствии с User Story и тест-кейсами.

---
### 1.1 Структура тест-кейса

В качестве примера рассмотрим тест-кейс TC_01.001.01:
- 01 — New Item (User Story)
- 001 — Create a new item (раздел / функциональность)
- 01 — Verify new item can be created from dashboard (конкретный сценарий)

---
### 1.2 Структура каталогов

В папке tests/ создаётся Python package, название которого:
- соответствует User Story
- начинается с префикса test_

Например:
```
tests/
└── test_new_item/
```
---
### 1.3 Структура файлов

Внутри папки User Story создаётся Python-файл:
- название соответствует разделу, который тестируется
- обязательно начинается с test_

Например:
```
tests/test_new_item/test_create_new_item.py
```
---
### 1.4 Структура тестового класса

Внутри тестового файла создаётся один тестовый класс, который:
- начинается с Test
- отражает проверяемый функционал
- не содержит логики и вспомогательных методов

Например:
```
class TestCreateNewItem:
    ...
```
---
### 1.5 Правила написания тестов
- Каждый тест — отдельная функция
- Название теста обязательно начинается с test_
- Название теста должно соответствовать тест-кейсу
- Один тест = один тест-кейс
- Номер тест кейса пишем в комментариях
- В тесте запрещено:
  - писать бизнес-логику
  - использовать локаторы
  - работать напрямую с WebDriver
  - генерировать данные

Например:
```
def test_verify_new_item_can_be_created(self, <аргументы>):
    """TC_01.001.01 | New Item > Create new Item > Verify new item can be created from dashboard"""
    ...
```
---
### 2. Структура Page Object Model (POM)
Вся логика работы с интерфейсом приложения хранится исключительно в папке pages/

Прямое взаимодействие с page, browser, context запрещено в тестах

Вся логика работы с UI выносится в pages/

---

### 2.1 Base Page

В папке pages/ обязательно присутствует файл:
```
pages/base_page.py
```

BasePage — базовый класс, от которого наследуются все PageObject

В BasePage хранятся:
- базовая работа с Playwright page
- общие методы взаимодействия с элементами
- ожидания
- универсальные проверки

📌 Примеры методов BasePage:
```
def open(url)

def click(locator)

def fill(locator, value)

def wait_for_visible(locator)

def is_visible(locator)

def get_text(locator)
```

🚫 В BasePage запрещена логика конкретных бизнес-разделов

---
### 2.2 PageObject для разделов
Для каждого раздела приложения создаётся отдельный PageObject:
- название файла соответствует US
- в конце названия обязательно добавляется page
- файл содержит всю логику работы с данным разделом

Например:
```
pages/
├── base_page.py
├── new_item_page.py
├── folder_management_page.py
└── ...
```

---
### 3. Locators

Все локаторы хранятся отдельно в папке:
```
locators/
```

### 3.1 Структура локаторов
Для каждого раздела создаётся файл локаторов:
- название соответствует рUS
- в конце обязательно добавляется locators
- файл содержит только локаторы
- логика и ассерты запрещены

Например:
```
locators/
├── new_item_locators.py
├── folder_management_locators.py
└── ...
```

---
### 4. Папка data (тестовые данные и дата-классы)

Вся информация, используемая в тестах, хранится исключительно в папке data/

Например:
```
data/
├── dataclasses/
│   ├── user_dataclass.py
│   ├── new_item_data.py
│   └── ...
├── endpoints.py
└── ...
```

---
### 4.1 Dataclasses

В папке data/dataclasses/ описываются структуры данных, которые используются в тестах и PageObject.

📌 Требования:
- только @dataclass
- никакой логики
- только описание данных

Например:
```
from dataclasses import dataclass

@dataclass
class CreateUserDataClass:
    username: str = None
    password: str = None
    confirm_password: str = None
    fullname: str = None
    email: str = None
```

---

### 4.2 Тестовые данные

В data также хранятся:
- статические тестовые данные
- константы
- эндпоинты

---
### 5 Папка utils (генераторы и ассерты)

Вся вспомогательная логика выносится в отдельную папку

Например:
```
utils/
├── generators/
│   ├── create_user_generator.py
│   ├── base_generator.py
│   └── ...
└── assertions/
    └── assertions.py
```

---
### 5.1 Генераторы данных

Генераторы используются для:
- создания валидных/невалидных данных
- подготовки тестовых сценариев

Например:
```
from data.dataclasses.create_user_dataclass import CreateUserDataClass
from data.generator.generator import Generator


class CreateUserGenerator(Generator):

    def create_user_generator(self):
        yield CreateUserDataClass(
            username=self.user_name,
            password=self.password,
            confirm_password=self.confirm_password,
            fullname=self.fullname,
            email=self.email
        )
```

---
### 5.2 Assertions

Например:
```
class Assertions:

    def assert_text(self, actual_text, expected_text):
        assert actual_text == expected_text, (f"Текст не соответствует ожидаемому тексту"
                                              f"Ожидали {expected_text}, получили {actual_text}")
```

---

### 6. Примерная архитектура

```
JenkinsQA_Python_2025_fall/
│
├── jenkins/
├── tests/
│   ├── README.md
│   ├── test_new_item 
│   │   ├── test_create_new_item.py
│   │   └── ...
│   └── ...
│
├── pages/
│   ├── base_page.py
│   ├── new_item_page.py
│   └── ...
│
├── locators/
│   ├── new_item_locators.py
│   ├── folder_management_locators.py
│   └── ...
│   
├── data/
│   ├── dataclasses/
│   │   ├── user_dataclass.py
│   │   ├── new_item_data.py
│   │   └── ...
│   ├── endpoints.py
│   └── ...
│
├── utils/
│   ├── generators/
│   │   ├── create_user_generator.py
│   │   ├── base_generator.py
│   │   └── ...
│   └── assertions/
│       └── assertions.py
│
├── docker-compose.yml
├── Dockerfile
├── requirements.txt
├── conftest.py
├── .github
├── pytest.ini
└── README.md
```